import axios from "axios";
import authHeader from './AuthHeader';

const API_BASE_URL = `${process.env.PUBLIC_API_BASE_PATH}/products`;


class ProductService {
    
}

export default new ProductService();